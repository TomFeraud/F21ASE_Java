package controllers;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import models.Queue;
import views.TestGUI;

/*public class TestController {

	private TestGUI view; // GUI to allow user to set the time

	private Queue queue; // clock model stores the time

	public TestController(TestGUI view, Queue queue) {
		this.queue = queue;
		this.view = view;
		// specify the listener for the view
		view.addSetListener(new SetListener());
	}

	// inner class SetListener responds when user sets the time
	public class SetListener implements ActionListener {
		public void actionPerformed(ActionEvent e) {
			int text = Integer.parseInt(view.getText());
			//queue.setTime24(hour, min);
		}
	}

} */
